import { Metadata } from "next";`nimport { sanityFetch } from "@/lib/sanity/client";`nimport { HOMEPAGE_QUERY } from "@/lib/sanity/queries";`nimport { getPrograms, getImpactMetrics } from "@/lib/api";`nimport HomeHero from "@/sections/HomeHero";`nimport ProgramsOverview from "@/sections/ProgramsOverview";`nimport ImpactMetricsSection from "@/components/ImpactMetricsSection";`nimport CTASection from "@/components/CTASection";`nimport SectionLabel from "@/components/ui/SectionLabel";`nimport NewsTicker from "@/components/NewsTicker";`nimport EventsScrollSection from "@/components/EventsScrollSection";`nimport CoreMembersSection from "@/components/CoreMembersSection";`nimport type { SanitySiteSettings } from "@/types";`n`nexport async function generateMetadata(): Promise<Metadata> {`n  return { title: "BICTA", description: "Bangladesh ICT Alliance" };`n}`n`nexport default async function HomePage() {`n  const [programs, metrics] = await Promise.allSettled([`n    getPrograms(true),`n    getImpactMetrics(),`n  ]);`n  return (`n    <>`n      <HomeHero />`n      <NewsTicker />`n      <EventsScrollSection />`n      <ProgramsOverview programs={(programs.status==="fulfilled"?programs.value:null)||[]} />`n      <ImpactMetricsSection metrics={(metrics.status==="fulfilled"?metrics.value:null)||[]} />`n      <CoreMembersSection />`n      <CTASection headline="Join the Movement" subtext="Whether you are a student, educator, or industry leader there is a place for you." primaryCta={{label:"Become a Partner",href:"/partners"}} secondaryCta={{label:"Explore Programs",href:"/programs"}} variant="gold-border" />`n    </>`n  );`n}`n
